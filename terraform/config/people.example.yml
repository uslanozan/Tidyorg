# =============================================================================
# people.yml — EXAMPLE SCHEMA · org MEMBERSHIP (NOT privilege)
# =============================================================================
# Copy this file to config/people.yml and fill it with your own members.
# Read by terraform/people.tf.
#
# Every name is an org member (default `member`). Ownership and org-scoped roles
# (head-of-engineering) are defined in config/privileged.yml — this file CANNOT
# EXPRESS PRIVILEGE (secure by construction). Machine-owned: the dashboard adds/
# removes members in this file.
#
# ⚠️ Names must be REAL GitHub usernames — adding a name produces a real org
# invitation. Do not write example/placeholder names; the ones here are only a
# schema example.
#
# Repo permissions (mentor/developer) live in config/repositories/*.yml, not here.
# =============================================================================

version: 1

members:
  - mentor-a
  - dev-1
  - dev-2
  - dev-3
