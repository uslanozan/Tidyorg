# =============================================================================
# people.yml — ÖRNEK ŞEMA · org ÜYELİĞİ (yetki DEĞİL)
# =============================================================================
# Bu dosyayı config/people.yml olarak kopyala ve kendi üyelerinle doldur.
# terraform/people.tf tarafından okunur.
#
# Her isim org üyesidir (varsayılan `member`). Owner'lık ve org kapsamlı roller
# (head-of-engineering) config/privileged.yml'da tanımlanır — bu dosya YETKİ İFADE
# EDEMEZ (secure by construction). Makine-sahipli: dashboard bu dosyaya üye ekler/
# çıkarır.
#
# ⚠️ İsimler GERÇEK GitHub kullanıcı adları olmalı — bir isim eklemek gerçek bir org
# daveti üretir. Örnek/placeholder isim yazma; buradakiler yalnızca şema örneğidir.
#
# Repo yetkisi (mentor/developer) config/repositories/*.yml içindedir, burada değil.
# =============================================================================

version: 1

members:
  - mentor-a
  - dev-1
  - dev-2
  - dev-3
