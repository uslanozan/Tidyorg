# The image contains ONLY the generic engine. Everything install-specific,
# mounted at runtime, or unrelated stays out.

# Config + state are mounted at runtime, never baked in.
terraform/config
terraform/*.tfstate
terraform/*.tfstate.*
terraform/.terraform

# Backend + imports are install-specific:
#   backend.tf   → your remote backend (e.g. HCP); the entrypoint writes a local backend
#   imports.tf   → imports a specific repo by name; breaks for any other org
terraform/backend.tf
terraform/imports.tf

# Dashboard is a separate image (dashboard/Dockerfile); it is not part of the
# engine build context.
dashboard

# Not part of the image.
docs
integrations
presentation
node_modules
**/node_modules

# Secrets must never enter the build context.
*.pem
*.key
*.tfvars
!*.tfvars.example
.env
.env.*

# VCS / tooling
.git
.github
.gitignore
Dockerfile
.dockerignore
docker-compose.yml
